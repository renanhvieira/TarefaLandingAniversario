{
  "name": "landing-page---tarefa",
  "version": "1.0.0",
  "scripts": {
    "cleanup": "rmdir /q /s .parcel-cache dist",
    "dev": "parcel src/index.html src/styles/main.scss src/scripts/main.js",
    "build": "parcel build src/index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "parcel": "^2.13.3"
  }
}
